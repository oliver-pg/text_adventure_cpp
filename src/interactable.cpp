#include "../include/interactable.h"

// We keep this destructor empty, as the base class doesn't need to do much.
// It's like having a peaceful exit with no attachments.
Interactable::~Interactable() {}
